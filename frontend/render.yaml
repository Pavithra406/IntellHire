services:
  - type: web
    name: intellhire-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://intellhire-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
